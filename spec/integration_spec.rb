require 'spec_helper'

RSpec.describe 'Top level integration spec' do
  describe 'run contentar [website]' do

    # let(:data_file) { 'data/example.com.csv' }
    # before { `ruby contentar #{ URL }` }
    # after { FileUtils.rm(data_file) }

    # it "generates a file with the site's data" do
    #   expect(File.file?(data_file)).to be
    # end
  end
end